<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Sisonke_Banking_App.SavingsAccountBalance">
    <ContentPage.Content>
        <StackLayout>
            <Label x:Name="accountShowID" FontSize="24" Padding="30,0,30,0"/>
            <Label x:Name="accountShowSavingsAccountBalance" FontSize="24" Padding="30,0,30,0"/>

            <Button
                Text="SavingsAccountBalance"
                Clicked="Button_Clicked" />
            <CollectionView x:Name="checkSavingsAccountBalance">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout>
                            <Label Text="{Binding Id}" FontSize="15" FontAttributes="Bold"/>
                            <Label Text="{Binding SavingsAccountBalance}" FontSize="Medium" FontAttributes="Bold"/>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>